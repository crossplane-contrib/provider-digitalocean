apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.0
  creationTimestamp: null
  name: doloadbalancers.loadbalancer.do.crossplane.io
spec:
  group: loadbalancer.do.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - do
    kind: DOLoadBalancer
    listKind: DOLoadBalancerList
    plural: doloadbalancers
    singular: doloadbalancer
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: A LB is a managed resource that represents a DigitalOcean LB.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: A LBSpec defines the desired state of a LB.
            properties:
              deletionPolicy:
                description: DeletionPolicy specifies what will happen to the underlying external when this managed resource is deleted - either "Delete" or "Orphan" the external resource. The "Delete" policy is the default when no policy is specified.
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                description: 'LBParameters define the desired state of a DigitalOcean LoadBalancer. Most fields map directly to a LoadBalancer: https://developers.digitalocean.com/documentation/v2/#load-balancers'
                properties:
                  algorithm:
                    description: 'Algorithm: The load balancing algorithm used to determine which backend Droplet will be selected by a client. It must be either "round_robin" or "least_connections". If not provided the default one will be used: "round_robin"'
                    enum:
                    - round_robin
                    - least_connections
                    type: string
                  forwardingRules:
                    description: An object specifying forward rules settings for the Load Balancer. If omitted, default values will be provided.
                    items:
                      description: DOForwardingRule define the DigitalOcean loadbalancers forward rules configurations.
                      properties:
                        certificateId:
                          description: The certificate id to use if using ssl
                          type: string
                        entryPort:
                          description: The entry port to be used.
                          type: integer
                        entryProtocol:
                          description: The entry protocol to be used. ie. tcp.
                          type: string
                        targetPort:
                          description: The target port to be used.
                          type: integer
                        targetProtocol:
                          description: The target protocol to be used. ie. tcp.
                          type: string
                        tlsPassthrough:
                          type: boolean
                      type: object
                    type: array
                  healthCheck:
                    description: An object specifying health check settings for the Load Balancer. If omitted, default values will be provided.
                    properties:
                      checkIntervalSeconds:
                        description: The number of seconds between between two consecutive health checks. The value must be between 3 and 300.
                        type: integer
                      healthyThreshold:
                        description: The number of times a health check must pass for a backend Droplet to be marked "healthy" and be re-added to the pool.
                        type: integer
                      port:
                        description: The port number to the healthcheck will connect.
                        type: integer
                      protocol:
                        description: The protocol to be used to perform the health check.
                        type: string
                      responseTimeoutSeconds:
                        description: The number of seconds the Load Balancer instance will wait for a response until marking a health check as failed.
                        type: integer
                      unhealthyThreshold:
                        description: The number of times a health check must fail for a backend Droplet to be marked "unhealthy" and be removed from the pool.
                        type: integer
                    type: object
                  region:
                    description: 'Region: The unique slug identifier for the region that you wish to deploy in.'
                    type: string
                  tags:
                    description: 'Tags: A flat array of tag names as strings to apply to the LB after it is created. Tag names can either be existing or new tags.'
                    items:
                      type: string
                    type: array
                  vpcUuid:
                    description: 'VPCUUID: A string specifying the UUID of the VPC to which the LB will be assigned. If excluded, beginning on April 7th, 2020, the LB will be assigned to your account''s default VPC for the region.'
                    type: string
                required:
                - region
                type: object
              providerConfigRef:
                description: ProviderConfigReference specifies how the provider that will be used to create, observe, update, and delete this managed resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - name
                type: object
              providerRef:
                description: 'ProviderReference specifies the provider that will be used to create, observe, update, and delete this managed resource. Deprecated: Please use ProviderConfigReference, i.e. `providerConfigRef`'
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace and name of a Secret to which any connection details for this managed resource should be written. Connection details frequently include the endpoint, username, and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: A LBStatus represents the observed state of a LB.
            properties:
              atProvider:
                description: A LBObservation reflects the observed state of a LB on DigitalOcean.
                properties:
                  creationTimestamp:
                    description: CreationTimestamp in RFC3339 text format.
                    type: string
                  id:
                    description: ID for the resource. This identifier is defined by the server.
                    type: string
                  ip:
                    description: IP for the resource.
                    type: integer
                  status:
                    description: "A Status string indicating the state of the LB instance. \n Possible values:   \"new\"   \"active\"   \"off\""
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True, False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
